-- Programs used by keybinds and rules. Required by the modules that need them,
-- so there is one place to change your terminal or browser.
return {
    terminal     = "kitty",
    file_manager = "thunar",
    menu         = "wofi --show drun",
    browser      = "firefox",
    editor       = "kitty -e nvim",
}
